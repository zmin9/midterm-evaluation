name: CD
on:
  push:
    branches:
      - main
jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Config user
        run: |
          git config --global user.email "o3o0903@gmail.com"
          git config --global user.name "zmin9"

      - run: yarn install

      - name: Build
        run: yarn build

      - name: Deploy
        run: yarn run deploy
